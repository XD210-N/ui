你好，你将接手一个正在进行的前端重构项目，请先完整读完这段话再动手。

【项目背景】这是一个 Next.js + React + Tailwind 的前端工程。甲方（数字动力）有一套面向他们自己工程师的测试前端（功能完整但界面工程化）。我方的任务是：在完全不破坏任何功能和前后端对接的前提下，只改 UI 的外观、布局、交互，做成面向真实用户的、苹果质感的高级界面。交付遵循 drop-in replacement 原则——我方代码覆盖回甲方工程后必须立刻能用。工程里有两个目录：`C:\XD\Git\mycode\pro_011\1ui-main\ui-main` 是甲方原始工程（baseline，带 1 前缀，还原和对比都以它为准）；`C:\XD\Git\mycode\pro_011\ui-main\ui-main` 是当前在改的工程（= 甲方原工程 + 我方新增的 app/redesign/ 目录）。我方所有新代码都在 app/redesign/ 里。

【甲方要求】以项目为核心：初始页面只有一个对话框，用户发消息后后端会分析需求并生成 UI 界面截图返回前端展示。甲方希望后端返回文字以外内容时，界面从"纯对话"自动变成"项目操作面板"形态：聊天窗优雅缩小移到左侧成为毛玻璃悬浮窗，中间浮现一个全屏的无边界网格画布（可拖拽平移、滚轮缩放、图片钉在画布上），右侧浮现信息展示窗（需求文档/方案/Screens截图），不能用生硬的左右分割。需求文档要流式输出、可关闭收起到角落再拽出。整体要高级、美观、苹果质感。甲方上一版反馈我方只是视觉原型、功能没接（发送按钮、对话运行时、预览画布、文件上传、历史记录都没接）——这些现在已经全部用甲方真实组件接好了。

【我的思路与要求】核心原则是视觉层（redesign/ui/ + CSS）和功能层（redesign/bindings/ + 甲方组件）严格分离：你在视觉层怎么改都安全；一旦碰到功能就只"接线"（调甲方已有的 hook/store/方法）不"重写"，只"用 CSS 隐藏"不"卸载组件"。绝对不能用 `{条件 && <组件>}` 卸载 Thread / ArtifactCanvas / MyRuntimeProvider / Composer 这些核心组件（会丢状态断订阅）。不能自己 fetch、不能造假数据混进真实 runtime。本地没有甲方后端，所以用 mock 模拟，但模拟必须严格走甲方既定的接口契约、只替换数据来源，目的是自证清白（证明前端接口代码没问题）。任何改动甲方原工程文件（app/redesign 以外）的地方，都必须记录到文档。每完成一步我要自己运行看效果、确认 OK 才进下一步，过程中没处理到的控件窗口要继续显示能用。

【必读文档，在 app/redesign/MD/ 下，按这个顺序读】第一份也是最重要的是《给下一个AI的接手提示.md》（唯一权威入口，讲清了背景、铁律、我做了什么、后续规划、已知陷阱）；然后《项目化交互重构执行方案.md》（当前主线任务，5 个阶段的分步计划和进度，目前阶段 0/1/2 已完成，下一步是阶段 3 需求文档流式窗）；《前端视觉交互修改方案手册.md》（想改某个视觉/交互效果时，查这里对应怎么改代码才不影响后端，表里没有的需求要先梳理安全改法再补进去）；《修改代码注意事项.md》（硬性底线）；《原工程代码修改说明.md》（所有改过甲方原文件的记录）；《副界面四视图说明.md》（右侧四个视图的数据来源、时机、格式）。对接和模拟相关看《甲方对接与模拟说明.md》《我方需求与模拟对接策略.md》《临时MOCK后端说明.md》《待确认信息.md》。注意：PROJECT_OVERVIEW.md、FRONTEND_ANALYSIS.md、MYWORK.md 这三份的布局描述已过时（是旧的三列布局），只看它们的原则部分，布局以执行方案为准。

【请你先做】完整读完上述文档后，先输出你对整个工程架构和当前进度的理解（区分清楚甲方原代码和我方 redesign 代码），确认你理解了铁律，然后告诉我你打算怎么继续推进阶段 3，等我确认后再动手。不要一上来就大改。
